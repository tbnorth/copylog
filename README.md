# CopyLog

Log everything that is copied to the clipboard. This is useful for debugging and for
keeping a history of what has been copied.

For now 2026-03-03 this only handles the logging part and will be called by another
script I'm already running with clipboard updates.  Maybe merge the two later.